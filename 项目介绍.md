# C-WebServer

基于 Linux 的 **C++17 高性能 HTTP 服务器**：Reactor 模型 + epoll 边缘触发 + 非阻塞 IO + 线程池，
实现 HTTP/1.1 协议处理、Keep-Alive 长连接管理、定时器超时回收、异步日志，并内置
**静态文件服务(sendfile 零拷贝)**、**路由表**、**内存 KV 存储**与**令牌桶限流**。

> 压测数据真实可复现（4 核 Ubuntu 22.04，ab 同机压测，全程 0 失败），见[压测数据](#压测数据)。

## 功能特性

- **HTTP/1.1 协议解析**：请求行/请求头/查询参数/请求体(Content-Length)，GET/POST/PUT/DELETE，
  完整返回 200/400/403/404/405/413/429/431/501 状态码
- **半包处理**：每连接输入缓冲跨事件累积，正确处理 TCP 粘包/拆包，支持 HTTP 流水线
- **静态文件服务**：`/static/*` 前缀路由，**sendfile 零拷贝**发送（文件不经过用户态缓冲），
  MIME 类型识别，路径穿越防护(拒绝 `..` 与编码绕过)
- **路由表**：精确路径、`:param` 参数路径、`/*` 前缀路径三种匹配模式；路径存在但方法不匹配返回 405
- **内存 KV 存储**：`PUT/GET/DELETE /kv/:key`，互斥锁保护哈希表，O(1) 读写
- **令牌桶限流**：按客户端 IP 限流，超限返回 429，可对指定路由开启
- **Keep-Alive 长连接**：HTTP/1.1 默认长连接，30 秒空闲超时自动回收
- **定时器**：红黑树(multiset)有序 + 哈希表 O(1) 定位 + 智能指针 + 惰性删除
- **异步日志**：单例 + 前后台双缓冲 + 等级过滤，队列满丢弃最旧、不阻塞业务线程
- **并发安全**：连接关闭幂等化(防 fd 复用误关)、输出段队列保证流水线响应顺序、
  eventfd 信号唤醒优雅退出、半关闭(EPOLLRDHUP)正确处理避免 RST

## 架构

单 Reactor 主线程负责事件分发，业务处理(读/解析/写)下沉到线程池：

```
┌────────────────── 主线程(事件循环) ──────────────────┐
│  epoll_wait(超时 = 最近定时器剩余时间)                 │
│   ├─ 监听fd   → accept 新连接, 注册 EPOLLIN|ET|RDHUP  │
│   ├─ 连接fd   → 提交读/写任务到线程池                  │
│   ├─ eventfd  → 收到 SIGINT/SIGTERM, 优雅退出         │
│   └─ 每次循环 → checkExpired() 触发超时回调            │
├──────────────── 线程池(核数个线程) ───────────────────┤
│  handleRead: recv 读尽 → 解析请求 → 路由分发 → 响应    │
│    ├─ 文本/小响应: 直接组装到输出段队列                │
│    └─ 静态文件: 响应头入队 + sendfile 零拷贝发送文件体  │
│  handleWrite: 按序冲刷输出段队列(EPOLLOUT 可写事件)    │
└──────────────────────────────────────────────────────┘
```

## 路由表

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | / | 首页文本 |
| GET | /about | 关于页 |
| GET | /static/* | 静态文件(sendfile 零拷贝, 路径穿越防护) |
| PUT | /kv/:key | 写入内存 KV(请求体为值) |
| GET | /kv/:key | 读取 KV 值 |
| DELETE | /kv/:key | 删除 KV 键 |
| GET | /kv | 列出全部键值 |
| POST | /echo | 回显请求体(示例: 开启令牌桶限流) |

新增接口只需在 `src/HttpServer.cpp` 的 `getRouter()` 中注册 handler。

## 目录结构

| 文件 | 职责 |
|------|------|
| src/main.cpp | 程序入口: 日志初始化、SIGPIPE 处理、启动服务器 |
| src/TcpServer.h/.cpp | Reactor 事件循环、连接管理(幂等关闭)、定时器接入、优雅退出 |
| src/HttpServer.h/.cpp | 每连接对象: 输入缓冲/输出段队列、路由分发、sendfile、背压保护 |
| src/Router.h/.cpp | 路由表: 精确/参数/前缀匹配、405 判定、限流开关 |
| src/KVStore.h/.cpp | 内存键值存储(互斥锁 + 哈希表) |
| src/RateLimiter.h/.cpp | 令牌桶限流器(按 IP) |
| src/HttpRequest.h/.cpp | 无状态请求解析器: 报文尺寸限制、Keep-Alive 判定 |
| src/HttpResponse.h/.cpp | 响应组装: 状态码 + 标准原因短语 + 文件响应标记 |
| src/ThreadPool.h/.cpp | 固定大小线程池: 互斥锁 + 条件变量 |
| src/TcpTimer.h/.cpp | 定时器管理器: multiset + unordered_map + 惰性删除 |
| src/TimerNode.h/.cpp | 定时器节点 |
| src/Logger.h/.cpp | 异步日志(单例): 双缓冲、等级过滤 |
| src/Util.h/.cpp | SIGPIPE 忽略、信号处理安装 |
| static/ | 静态文件示例(index.html / test.txt / big.bin) |

## 构建与运行

```bash
# 构建
bash build.sh                      # 等价于 cmake -S . -B build && cmake --build build -j
# 运行(建议从项目根目录运行, 以便定位到 static/)
./out/webserver                    # 默认监听 127.0.0.1:8848
./out/webserver 0.0.0.0 8080       # 指定 IP 与端口
# 测试
curl -i http://127.0.0.1:8848/
curl -i http://127.0.0.1:8848/about
curl -i -X POST -d "hello" http://127.0.0.1:8848/echo
curl -i http://127.0.0.1:8848/static/index.html
curl -i -X PUT -d "hello" http://127.0.0.1:8848/kv/greeting
curl -i http://127.0.0.1:8848/kv/greeting
# 压测
ab -k -c 500 -n 500000 http://127.0.0.1:8848/
```

日志输出到可执行文件同目录的 server.log；发送 SIGINT/SIGTERM 优雅退出。

## 压测数据

- 环境：Ubuntu 22.04.5，4 vCPU / 3.8GB RAM，g++ 11.4.0，-O2
- 工具：ab（同机压测，压测本身也占用 CPU）
- 结果：全部场景 **0 failed requests**

### 小响应场景（内存拼装 vs sendfile）

| 场景 | 并发 | QPS | 吞吐 | p50 | p99 |
|------|------|-----|------|-----|-----|
| 文本 GET /（36B，内存拼装） | 500 | 7899 | 1.0 MB/s | 62ms | 105ms |
| 静态 index.html（238B，sendfile） | 500 | 8040 | 2.6 MB/s | 61ms | 102ms |

### 1MB 大文件（sendfile 零拷贝）

| 并发 | QPS | 吞吐 | p50 | p99 |
|------|-----|------|-----|-----|
| 100 | 1094 | **1.12 GB/s** | 88ms | 134ms |
| 500 | 1029 | **1.05 GB/s** | 476ms | 613ms |

**结论**：sendfile 使文件内容直接在内核态从页缓存 DMA 到网卡，不经过用户态内存拷贝，
1MB 文件吞吐可达 1.1GB/s；而文本响应受限于每次请求的串行处理链路，QPS 更高但吞吐受限。
这验证了零拷贝对"大响应 + 高吞吐"场景的核心价值。

## 关键设计(面试点)

- **半包/粘包**：每连接独立输入缓冲 + 按 \r\n\r\n + Content-Length 判定完整请求，支持流水线；
  请求行/头部/体均有尺寸上限，防内存耗尽
- **关闭幂等化**：close 前先从连接表原子移除，任意线程重复关闭同一 fd 直接返回，杜绝 fd 复用误关
- **半关闭处理**：EPOLLRDHUP 时**不直接关闭**——接收队列可能仍有未读数据，直接 close 会触发
  RST；改为提交读任务，recv 读到 0 后干净关闭（压测中发现并修复的经典问题）
- **输出段队列**：文本段与文件段(sendfile)按序入队发送，保证流水线下多响应顺序正确，
  并带队列长度上限做背压保护
- **sendfile 零拷贝**：文件响应 = 响应头(文本段) + 文件内容(文件段, sendfile 发送)，
  文件 fd 生命周期随连接管理，断开时统一回收
- **令牌桶限流**：桶容量控制突发、固定速率补充令牌，O(1) 判断，按 IP 隔离
- **优雅退出**：eventfd 注册进 epoll，SIGINT/SIGTERM 处理器写 eventfd 唤醒事件循环

## 已知限制与后续方向

- 暂不支持 chunked 传输编码、HTTPS/TLS
- 单 Reactor 主线程为事件分发单点，下一步可改造为 one loop per core 多事件循环
- KV 存储无持久化/过期策略；限流为进程内状态，多实例部署需外部存储
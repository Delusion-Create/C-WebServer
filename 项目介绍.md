# C-WebServer

基于 Linux 的 **C++17 高性能 HTTP 服务器**：Reactor 模型 + epoll 边缘触发 + 非阻塞 IO + 线程池，
实现完整的 HTTP/1.1 请求处理、Keep-Alive 长连接管理、定时器超时回收与异步日志。

> 压测数据真实可复现（4 核 Ubuntu 22.04，ab 同机压测，全程 0 失败），见[压测数据](#压测数据)。

## 功能特性

- **HTTP/1.1 协议解析**：请求行 / 请求头 / 查询参数 / 请求体(Content-Length)，支持 GET/POST，
  完整返回 200/400/404/405/413/431/501 状态码
- **半包处理**：输入缓冲跨事件累积，正确处理 TCP 粘包/拆包，支持 HTTP 流水线(单连接多请求)
- **Keep-Alive 长连接**：HTTP/1.1 默认长连接，30 秒空闲超时自动回收；Connection: close 主动断开
- **定时器**：红黑树(multiset)维护过期时间有序性 + 哈希表 O(1) 定位 fd + 智能指针 + 惰性删除
- **异步日志**：单例 + 前后台双缓冲 + 条件变量，日志等级过滤，队列满时丢弃最旧、不阻塞业务线程
- **线程池**：任务队列 + 条件变量，线程数自动适配 CPU 核数
- **并发安全**：连接关闭幂等化（杜绝 fd 复用后误关）、锁序一致无死锁、eventfd 信号唤醒优雅退出

## 架构

单 Reactor 主线程负责事件分发，业务处理(读/解析/写)下沉到线程池：

```
┌────────────────── 主线程(事件循环) ──────────────────┐
│  epoll_wait(超时 = 最近定时器剩余时间)                 │
│   ├─ 监听fd   → accept 新连接, 注册 EPOLLIN|ET|RDHUP  │
│   ├─ 连接fd   → 提交读/写任务到线程池                  │
│   ├─ eventfd  → 收到 SIGINT/SIGTERM, 优雅退出         │
│   └─ 每次循环 → checkExpired() 触发超时回调            │
├──────────────── 线程池(核数个线程) ───────────────────┤
│  handleRead: recv 读尽 → 解析完整请求 → 构造响应 → 发送 │
│  handleWrite: 冲刷输出缓冲(EPOLLOUT 可写事件)          │
└──────────────────────────────────────────────────────┘
```

## 目录结构

| 文件 | 职责 |
|------|------|
| src/main.cpp | 程序入口: 日志初始化、SIGPIPE 处理、启动服务器 |
| src/TcpServer.h/.cpp | Reactor 事件循环、连接管理(幂等关闭)、定时器接入、优雅退出 |
| src/HttpServer.h/.cpp | 每连接一个对象: 输入/输出缓冲、半包解析、路由与响应、EPOLLOUT 冲刷 |
| src/HttpRequest.h/.cpp | 无状态请求解析器: 报文尺寸限制、Keep-Alive 判定、错误码区分 |
| src/HttpResponse.h/.cpp | 响应组装: 状态码 + 标准原因短语 + 头 + 体 |
| src/ThreadPool.h/.cpp | 固定大小线程池: 互斥锁 + 条件变量 |
| src/TcpTimer.h/.cpp | 定时器管理器: multiset + unordered_map + 惰性删除 |
| src/TimerNode.h/.cpp | 定时器节点: 过期时间、回调、删除标记 |
| src/Logger.h/.cpp | 异步日志(单例): 双缓冲、等级过滤、队列上限 |
| src/Util.h/.cpp | SIGPIPE 忽略、信号处理安装 |

## 构建与运行

```bash
# 构建
bash build.sh                      # 等价于 cmake -S . -B build && cmake --build build -j
# 运行
./out/webserver                    # 默认监听 127.0.0.1:8848
./out/webserver 0.0.0.0 8080       # 指定 IP 与端口
# 测试
curl -i http://127.0.0.1:8848/
curl -i http://127.0.0.1:8848/about
curl -i -X POST -d "hello" http://127.0.0.1:8848/echo
# 压测
ab -k -c 500 -n 500000 http://127.0.0.1:8848/
```

日志输出到可执行文件同目录的 server.log；发送 SIGINT/SIGTERM 优雅退出。

## 压测数据

- 环境：Ubuntu 22.04.5，4 vCPU / 3.8GB RAM，g++ 11.4.0，-O2
- 工具：ab（同机压测，压测本身也会占用 CPU，实际吞吐上限会更高）
- 结果：全部场景 **0 failed requests**

| 场景 | 并发 | 总请求 | QPS | p50 | p90 | p99 |
|------|------|--------|-----|-----|-----|-----|
| Keep-Alive | 100 | 200,000 | 7150 | 14ms | 20ms | 25ms |
| Keep-Alive | 500 | 500,000 | 6647 | 79ms | 107ms | 156ms |
| Keep-Alive | 1000 | 1,000,000 | 6894 | 152ms | 194ms | 257ms |
| 短连接(每次新建) | 200 | 100,000 | 6694 | 29ms | 44ms | 55ms |

并发从 100 提升到 1000，QPS 基本稳定在 6500~7200，说明吞吐瓶颈不在并发连接数，而在
单事件循环 + 每请求一次 recv/解析/send 的串行链路上（4 核机器上服务器进程约占 1.25 核）。

## 关键设计(面试点)

- **半包/粘包**：每个连接持有独立的输入缓冲，解析器按 \r\n\r\n + Content-Length 判定
  "完整请求"，跨事件累积，天然支持流水线；同时对请求行/头部/体做尺寸上限，防止内存耗尽
- **关闭幂等化**：close 路径先原子地从连接表移除，再执行 epoll_del/close，任意线程(事件循环/
  线程池/定时器回调)再次关闭同一 fd 直接返回，杜绝 fd 复用后误关新连接
- **定时器**：multiset(红黑树)保证按过期时间有序，取最近超时 O(1)；哈希表按 fd 索引 O(1) 增删；
  惰性删除标记避免并发环境下直接析构仍在引用的节点
- **异步日志**：生产者只写前台缓冲 + 唤醒条件变量；后台线程交换缓冲后批量落盘，
  把磁盘 IO 移出请求关键路径；队列满时丢弃最旧日志，保证业务线程永不阻塞
- **优雅退出**：eventfd 注册进 epoll，SIGINT/SIGTERM 处理器向 eventfd 写 1 字节唤醒事件循环，
  干净退出并回收线程池

## 已知限制与后续方向

- 暂不支持 chunked 传输编码、静态文件服务、HTTPS/TLS
- 单 Reactor 主线程为事件分发单点，下一步可改造为 one loop per core 多事件循环
- POST 请求体上限 16MB（可配置）
- 未引入连接级背压与写缓冲上限，超大响应体可能占用较多内存
